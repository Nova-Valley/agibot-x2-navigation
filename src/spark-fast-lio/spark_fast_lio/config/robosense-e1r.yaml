/**:
  ros__parameters:
    preprocess:
      lidar_type: 2               # 1 for Livox serials LiDAR, 2 for Velodyne LiDAR, 3 for ouster LiDAR,
      scan_line: 6
      scan_rate: 10               # only need to be set for velodyne, unit: Hz,
      timestamp_unit: 3           # the unit of time/t field in the PointCloud2 rostopic: 0-second, 1-milisecond, 2-microsecond, 3-nanosecond.
      blind: 2.5
      blind_for_human_pilot: 2.5  # This parameter is to reject cloud points from human pilots

    mapping:
      acc_cov: 0.1
      gyr_cov: 0.1
      b_acc_cov: 0.0001
      b_gyr_cov: 0.0001
      fov_degree: 120.0
      det_range: 5.0
      extrinsic_est_en: false      # true: enable the online estimation of IMU-LiDAR extrinsic,

      extrinsic_T: [ -0.1039, 0.0596, 0.0388, ]
      extrinsic_R: [ 0.9986, 0.0166, 0.0496,
                     -0.0174, 0.9997, 0.0166,
                     -0.0493, -0.0174, 0.9986,]

    filter_size_map: 0.15

    publish:
      path_en: true
      scan_publish_en: true        # false: close all the point cloud output
      dense_publish_en: true       # false: low down the points number in a global-frame point clouds scan.
      scan_lidarframe_pub_en: true # true: output the point cloud scans in the LiDAR frame
      scan_bodyframe_pub_en: true  # true: output the point cloud scans in IMU-body frame
      scan_baseframe_pub_en: false  # true: output the point cloud scans in the base frame

    pcd_save:
      pcd_save_en: true
      interval: 50                # how many LiDAR frames saved in each pcd file;
      # -1 : all frames will be saved in ONE pcd file, may lead to memory crash when having too much frames.

    verbose: true
    runtime_pos_log_enable: false
    point_filter_num_for_preprocessing: 1 # If it is set to `1`, full point cloud gonna be publlished
    point_filter_num: 4                   # For pose estimation.
    max_iteration: 4
    cube_side_length: 80.0
